
name: Haskell Stack Tests

on:
  - push
  - pull_request
permissions:
  checks: write
  actions: read
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: Install Stack
       run: stack upgrade

     - name: Run Stack tests
       run: stack --test
